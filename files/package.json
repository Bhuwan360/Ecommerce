{
  "name": "halide-catalog",
  "version": "1.0.0",
  "private": true,
  "description": "Halide — a film & darkroom supply catalog. Web development capstone: modular vanilla-JS SPA with client-side routing, deployed to Vercel.",
  "type": "module",
  "scripts": {
    "dev": "npx --yes serve . -l 5173",
    "build": "node build.js",
    "preview": "npx --yes serve dist -l 4173"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "esbuild": "^0.23.1",
    "html-minifier-terser": "^7.2.0"
  }
}
